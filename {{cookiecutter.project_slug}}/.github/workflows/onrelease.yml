name: Release Pipeline

on:
  release:
    types:
      - created

jobs:
  release:

    runs-on: ubuntu-latest

    env:
      MLFLOW_TRACKING_URI: 'databricks'
      DATABRICKS_HOST: {{ "${{ secrets.DATABRICKS_HOST }}" }}
      DATABRICKS_TOKEN: {{ "${{ secrets.DATABRICKS_TOKEN }}" }}

    steps:
      - uses: actions/checkout@v1

      - name: Announce
        run: |
          echo "Running ONRELEASE GitHub Action"

      - name: Install pip
        run: |
          python -m pip install --upgrade pip

      - name: Install dbx
        run: |
          pip install -U tools/dbx-0.7.0-py3-none-any.whl

      - name: Prepare profile
        run: |
          echo "[DEFAULT]" >> ~/.databrickscfg
          echo "host = $DATABRICKS_HOST" >> ~/.databrickscfg
          echo "token = $DATABRICKS_TOKEN" >> ~/.databrickscfg

      - name: Deploy acceptance tests
        run: |
          dbx deploy --jobs={{cookiecutter.corning_division.lower()}}_{{cookiecutter.project_slug}}_sample_acc_test --deployment-file=.dbx/deployment.json --requirements-file=.dbx/requirements.txt

      - name: Run acceptance tests
        run: |
          dbx launch --job={{cookiecutter.corning_division.lower()}}_{{cookiecutter.project_slug}}_sample_acc_test --trace --existing-runs=wait

      - name: Deploy the production job
        run: |
          dbx deploy --jobs={{cookiecutter.corning_division.lower()}}_{{cookiecutter.project_slug}}_sample_production --deployment-file=.dbx/deployment.json --requirements-file=.dbx/requirements.txt
