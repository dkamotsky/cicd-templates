version: "3.8"

services:
  test:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: connect
    entrypoint:
      - /{{cookiecutter.project_slug}}/tests/main.sh
    volumes:
     - ..:/{{cookiecutter.project_slug}}

