version: "3.8"

services:
  run:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: connect
    entrypoint:
      - /{{cookiecutter.project_slug}}/docker/scripts/launch_dbx.sh
    command:
      - $ARGS
    volumes:
     - ..:/{{cookiecutter.project_slug}}


